query MyActivities($pageNumber: Int) {
  myActivities(input: { pageNumber: $pageNumber }) {
    items {
      name
      date
      type
      routes {
        grade
      }
    }
    meta {
      itemCount
      pageCount
      pageNumber
      pageSize
    }
  }
}
