query MyActivityRoutes($input: FindActivityRoutesInput) {
  myActivityRoutes(input: $input) {
    items {
      difficulty
      date
      ascentType
      grade
      notes
      partner
      publish
      activity {
        id
      }
      route {
        crag {
          id
          name
          slug
          country {
            slug
          }
        }
        name
        id
      }
    }
    meta {
      itemCount
      pageCount
      pageNumber
      pageSize
    }
  }
}
