query RouteComments($routeId: String!) {
  route(id: $routeId) {
    comments {
      id
      type
      user {
        firstname
        lastname
      }
      content
      updated
      created
    }
  }
}
