query RouteGrades($routeId: String!) {
  route(id: $routeId) {
    id
    difficulty
    name
    grade
    length
    grades {
      id
      grade
      user {
        firstname
        lastname
      }
      created
      updated
      isBase
      includedInCalculation
    }
  }
}
