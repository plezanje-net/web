query RouteGrades($routeId: String!) {
  route(id: $routeId) {
    id
    difficulty
    name
    grade
    length
    grades {
      grade
      user {
        firstname
        lastname
      }
      created
      updated
    }
  }
}
