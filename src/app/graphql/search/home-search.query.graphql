query HomeSearch($query: String) {
  search(input: $query) {
    name
    type
    crag {
      slug
      country {
        slug
      }
    }
    route {
      id
      difficulty
      grade
      crag {
        name
        slug
        country {
          slug
        }
      }
    }
  }
}
