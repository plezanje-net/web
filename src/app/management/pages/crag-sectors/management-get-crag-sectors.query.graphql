query ManagementGetCragSectors($id: String!) {
  crag(id: $id) {
    sectors {
      id
      label
      name
      publishStatus
      position
      routes {
        id
      }
      user {
        id
      }
    }
    slug
    name
    id
    country {
      name
      id
      slug
    }
    publishStatus
    user {
      id
    }
  }
}
