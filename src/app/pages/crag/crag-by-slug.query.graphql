query CragBySlug($crag: String!) {
  cragBySlug(slug: $crag) {
    id
    slug
    name
    orientation
    lat
    lon
    access
    description
    area {
      id
      name
    }
    country {
      id
      name
      slug
    }
    sectors {
      id
      name
      label
      routes {
        id
        name
        grade
        length
        comments {
          id
        }
      }
    }
    comments {
      id
      content
      created
      type
      user {
        id
        fullName
      }
    }
    images {
      id
      title
      path
    }
  }
}
