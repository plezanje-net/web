query RouteDifficultyVotes($routeId: String!) {
  route(id: $routeId) {
    id
    slug
    difficulty
    defaultGradingSystem {
      id
    }
    name
    length
    difficultyVotes {
      id
      difficulty
      user {
        firstname
        lastname
      }
      created
      updated
      isBase
      includedInCalculation
    }
  }
}
