query RouteGrades($routeId: String!) {
  route(id: $routeId) {
    id
    slug
    difficulty
    name
    length
    difficultyVotes {
      id
      user {
        firstname
        lastname
      }
      created
      updated
      isBase
      includedInCalculation
    }
  }
}
