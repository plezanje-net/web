query RouteGrades($routeId: String!) {
  route(id: $routeId) {
    id
    slug
    difficulty
    name
    length
    difficultyVotes {
      user {
        firstname
        lastname
      }
      difficulty
      created
      updated
      isBase
      includedInCalculation
    }
  }
}
