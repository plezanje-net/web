query LatestImages($latest: Int!) {
  latestImages(latest: $latest) {
    path
    title
    user {
      fullName
    }
    crag {
      id
      nrRoutes
      name
      slug
      country {
        slug
      }
    }
    route {
      id
      name
      crag {
        id
        nrRoutes
        name
        slug
        country {
          slug
        }
      }
    }
  }
}
