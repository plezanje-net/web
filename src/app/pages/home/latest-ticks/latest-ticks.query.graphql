query LatestTicks($latest: Int!) {
  latestTicks(latest: $latest) {
    activity {
      date
      type
    }
    ascentType
    route {
      difficulty
      name
      crag {
        name
        country {
          slug
          name
        }
        slug
      }
      id
    }
    user {
      fullName
      gender
    }
  }
}
