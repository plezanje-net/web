query LatestTicks($latest: Int!) {
  latestTicks(latest: $latest) {
    activity {
      date
      type
    }
    ascentType
    route {
      grade
      name
      crag {
        id
        nrRoutes
        name
        country {
          slug
          name
        }
        slug
      }
      id
    }
    user {
      fullName
      gender
    }
  }
}
